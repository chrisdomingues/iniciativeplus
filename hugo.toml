#baseurl = "https://devcows.github.io/hugo-universal-theme/" # Configure URL. if using gh-pages https://github.com/<username>/hugo-universal-theme URL: https://<username>.github.io/hugo-universal-theme/
baseURL = 'https://example.org/'
title = "IniciativePLUS"
theme = "universal"
languageCode = "pt-pt"
# Site language. Available translations in the theme's `/i18n` directory.
defaultContentLanguage = "pt-pt"

# number of words of summarized post content (default 70)
summaryLength = 70

# not pluralize title pages by default
pluralizelisttitles = false

[services]
    # Enable comments by entering your Disqus shortname
    [services.disqus]
        shortname = "devcows"
    # Enable Google Analytics by entering your tracking code
    [services.googleAnalytics]
        id = ""

# Define the number of posts per page
[pagination]
    pagerSize = 10

# Main menu
[menu]
[[menu.main]]
    name       = "Formação & Consultoria"
    identifier = "menu.formacaoeconsultadoria"
    url        = "/formacaoeconsultadoria/"
    weight     = 1

[[menu.main]]
    name       = "Suporte à Venda & Merchandasing"
    identifier = "menu.vendaemerchandising"
    url        = "/vendaemerchandising/"
    weight     = 2

[[menu.main]]
    name       = "Eventos"
    identifier = "menu.eventos"
    url        = "/eventos/"
    weight     = 3

[[menu.main]]
    name       = "Outsorcing"
    identifier = "menu.outsorcing"
    url        = "/outsorcing/"
    weight     = 4

[[menu.main]]
    name       = "Promotoras"
    identifier = "menu.promotoras"
    url        = "/promotoras/"
    weight     = 5

[[menu.main]]
    name       = "Contactos"
    identifier = "contactos"
    url        = "/contact/"
    weight     = 7


# Top bar social links menu

[[menu.topbar]]
    weight = 1
    name = "Facebook"
    url = "https://facebook.com"
    pre = "<i class='fab fa-2x fa-facebook'></i>"

[[menu.topbar]]
    weight = 2
    name = "Instagram"
    url = "https://instagram.com"
    pre = "<i class='fab fa-2x fa-instagram'></i>"


[params]
    viewMorePostLink = "/blog/"
    author = "DevCows"
    defaultKeywords = ["devcows", "hugo", "go"]
    mainSections = ["blog"]
    defaultDescription = "Site template made by Devcows using Hugo"

    # Social media
    facebook_site = "" # the Facebook handle of your site ('https://www.facebook.com/HANDLE')
    twitter_site = "GoHugoIO" # the Twitter handle of your site (without the '@')
    default_sharing_image = "img/sharing-default.png"

    # Google Maps widget: If `googleMapsApiKey` is not set, no key will be passed to Google (which likely results in a broken map widget).
    enableGoogleMaps = true
    googleMapsApiKey = "AIzaSyAv7Sza8NSp9_l_g8G2vlo0H4ydEPn_2jY"

    latitude = "38.7171638"
    longitude = "-9.2425421"

    # Style options: default (light-blue), blue, green, marsala, pink, red, turquoise, violet
    style = "blue"

    # Since this template is static, the contact form uses www.formspree.io as a
    # proxy. The form makes a POST request to their servers to send the actual
    # email. Visitors can send up to a 50 emails each month for free.
    #
    # What you need to do for the setup?
    #
    # - register your account to https://formspree.io/register
    # - login and create new form
    # - set your form's endpoint url under 'formspree_action' below
    # - upload the generated site to your server
    # - test a dummy email yourself
    # - you're done. Happy mailing!
    #
    # Enable the contact form by entering your Formspree.io endpoint url
    formspree_action = "https://formspree.io/sample/of/endpoint"
    contact_form_ajax = false

    # Formspree form supports Google reCAPTCHA Key (type v2).
    # If you use this feature, you should enable reCAPTCHA feature in the Formspree dashboard.
    #
    # By default, Formspree use a redirect page for recaptcha widget.
    # If you use a recaptcha widget in your contact page, you should do next steps.
    # (if you don't want, skip these steps)
    #
    #   1. register your site on Google recaptcha admin page: https://www.google.com/recaptcha/admin
    #   2. select reCAPTCHA v2 and checkbox widget type.
    #   3. remember site key and secret key.
    #   4. enter secret key into "Custom reCAPTCHA Key" field in your Formspree form setting page.
    #   5. change `enableRecaptchaInContactForm` is to true
    #   6. enter site key into `googleRecaptchaKey` to enable a recaptcha widget in your page.
    #
    enableRecaptchaInContactForm = false
    googleRecaptchaKey = "site_key_for_google_recaptcha"

    about_us = "<p>A INICIATIVE PLUS tem como principal missão a disponibilização de serviços que primam pela excelência, desenvolvemos e Implementamos soluções que se baseiam numa estratégia totalmente concebida para as necessidades dos nossos clientes, gerando assim fortes mais-valias.</p>"
    copyright = "Copyright (c) 2025 - 2026, IniciativePLUS; todos os direitos reservados."

    # Format dates with Go's time formatting
    date_format = "January 2, 2006"

    dropdown_mouse_over = false

    disabled_logo = false
    logo_text = "IniciativePLUS"

    logo = "img/logo_ip2.png"
    logo_small = "img/logo_ip2.png"
    contact_url = "/contact"
    address = """<p class="text-uppercase"><strong>IniciativePLUS</strong>
        <br>Rua Brasil, 22
        <br>2795-124
        <br>Linda-a-Velha
        <strong>Portugal</strong>
      </p>
      """

[permalinks]
    blog = "/blog/:year/:month/:day/:contentbasename/"

# Enable or disable top bar with social icons
[params.topbar]
    enable = true
    text = """<p class="hidden-sm hidden-xs">Contacte-nos +351 214 190 331 ou geral@iniciativeplus.com.</p>
      <p class="hidden-md hidden-lg"><a href="tel:+351 214 190 331" data-animate-hover="pulse"><i class="fas fa-phone"></i></a>
      <a href="mailto:geral@iniciativeplus.com" data-animate-hover="pulse"><i class="fas fa-envelope"></i></a>
      </p>
      """

# Enable and disable widgets for the right sidebar
[params.widgets]
    categories = true
    tags = true
    search = true

[params.carouselCustomers]
    items = 6
    auto_play = false
    slide_speed = 2000
    pagination_speed = 1000

[params.carouselTestimonials]
    items = 4
    auto_play = false
    slide_speed = 2000
    pagination_speed = 1000

[params.carouselHomepage]
    # All carousel items are defined in their own files. You can find example items
    # at 'exampleSite/data/carousel'.
    # For more information take a look at the README.
    enable = true
    auto_play = true
    slide_speed = 2000
    pagination_speed = 1000

[params.features]
    enable = true
    cols = 3 # Default: 3, Available values 2,3,4,6
    # All features are defined in their own files. You can find example items
    # at 'exampleSite/data/features'.
    # For more information take a look at the README.

[params.testimonials]
    enable = true
    # All testimonials are defined in their own files. You can find example items
    # at 'exampleSite/data/testimonials'.
    # For more information take a look at the README.
    title = "Valores que definem a INICIATIVE PLUS"
    subtitle = "Disponibilizamos um vasto leque de serviços com uma premissa de excelência, que nos permite adaptar as nossas práticas às reais necessidades dos nossos clientes."

[params.see_more]
    enable = true
    icon = "far fa-file-alt"
    title = "Recrutamento"
    subtitle = "Se pensa que o seu perfil se enquadra nos nossos serviços, ou se procura uma nova oportunidade profissional não hesite em nos contactar."
    link_url = "/contact"
    link_text = "Contacte-nos"

[params.clients]
    enable = true
    # All clients are defined in their own files. You can find example items
    # at 'exampleSite/data/clients'.
    # For more information take a look at the README.
    title = "Nossos Clientes"
    subtitle = "Contamos com uma equipa multidisciplinar, e estamos aptos a implementar e desenvolver as soluções que melhor se adequam aos nossos clientes."

[params.recent_posts]
    enable = false
    title = "From our blog"
    subtitle = "Pellentesque habitant morbi tristique senectus et netus et malesuada fames ac turpis egestas. Vestibulum tortor quam, feugiat vitae, ultricies eget, tempor sit amet, ante. Donec eu libero sit amet quam egestas semper. Aenean ultricies mi vitae est. Mauris placerat eleifend leo."
    hide_summary = false

[params.footer.recent_posts]
    enable = false

[taxonomies]
    category = "categories"
    tag = "tags"
    author = "authors"
